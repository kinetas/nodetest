import 'package:flutter/material.dart';

class FriendSearchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('친구 검색'),
      ),
      body: Center(
        child: Text('친구 검색 화면입니다.'),
      ),
    );
  }
}